<%= simple_form_for [@configuration, @ordenation], html: { class: 'form-horizontal' } do |f| %>
  <%= f.input :priority %>
  <%= f.input :field %>
  <%= f.input :direction %>
  <%= f.button :submit %>

<% end %>
